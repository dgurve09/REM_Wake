# Weekly Technical Record

**Week:** 2026-08-31 to 2026-09-06
**Work performed:** 2026-08-31 and 2026-09-06
**Project phase:** Block 7 paired PSG-to-wearable transfer
**Raw data acquired:** No
**Model training performed:** No
**Validation or test data accessed:** No

## 1. Technological Uncertainty

The Block 7 channel audit established that all recordings contain the fixed `PSG-6`, `PSG-2`, and `HB-2` channels, but it did not establish that their feature paths are mechanically comparable. Separate normalization, epoch-retention, spectral, or feature-order behavior could create an apparent PSG-to-wearable difference unrelated to the research question.

The feature gate therefore tested whether the frozen three-path implementation preserves common timing and schema, produces identical overlapping PSG features, and reproduces the earlier wearable feature construction before any transfer model is fitted.

## 2. Method

The implementation was committed on August 31 and executed on September 6 under the previously committed feature-validation plan. It:

- reconstructed 82 train recordings across 64 `pid` groups from the frozen split;
- read only train PSG/headband signals;
- applied the fixed 0.3-35 Hz filter, 256-to-128 Hz resampling, and Welch log-bandpower method;
- fitted six PSG and two wearable robust channel scalers using deterministic 1 Hz train samples;
- reused the six-channel PSG `F3/F4` scaler parameters for the reduced PSG path;
- generated `PSG-6`, `PSG-2`, and `HB-2` base features and eight-epoch context centers;
- compared overlapping PSG paths at tolerance `1e-10` and the wearable path with the frozen reference at tolerance `1e-6`; and
- wrote full feature arrays outside Git while retaining compact checks and SHA-256 manifests.

An independent read-only validator was committed before use. It did not read raw EDF files. It reconstructed train membership, rejected non-train rows, checked exact ordered schemas, rehashed and reopened every stored artifact, and recomputed the cross-path controls.

## 3. Result

| Check | Result |
|---|---:|
| Train signal paths | 164/164 passed |
| Synthetic band checks | 5/5 passed |
| Recording feature/context checks | 82/82 passed |
| `PSG-6`/`PSG-2` overlap maximum absolute difference | 0 |
| `HB-2` reproduction maximum absolute difference | `4.76619863576e-7` |
| External feature arrays recorded | 246 |
| Feature-gate checks | 13/13 passed |
| Independent integrity checks | 15/15 passed twice |

The independent stored-array comparison reproduced the PSG overlap difference of zero. Its float32 stored `HB-2` arrays were exactly equal to the frozen float32 reference arrays; the nonzero `4.7662e-7` value above is the stricter pre-storage float64-to-reference comparison from the primary gate.

## 4. Correction and Reproducibility

The August 31 implementation initially encoded August 31 as the intended result-folder date, but it had not been executed and no result existed. Before execution, the output date was corrected to the actual September 6 run date without changing the method or pass criteria. The result README records the protocol, plan, initial implementation, and execution-code commits separately.

The independent validator was run twice. On the second run, its reviewed outputs were verified as unchanged rather than overwritten.

## 5. Interpretation and Decision

The gate passed. No detected preprocessing-path, feature-order, epoch-timing, or external-artifact integrity problem blocks the paired transfer experiment. This is a feature-infrastructure result only: no event model, probability, threshold, validation metric, or test result was produced.

Block 7 is not complete by its planned September 6 end date. Direct and zero-shot transfer fitting and validation remain carryover work and must be completed before Block 8 robustness or adaptation experiments begin.

## 6. Next Work

1. implement the fixed `P6-D`, `P2-D`, `H2-D`, and `P2-H2-Z` train/validation comparison;
2. evaluate the two-part zero-shot adaptation gate without opening the test partition;
3. run the single permitted robust alignment only if that gate opens;
4. freeze model hashes, thresholds, comparator roles, and the adaptation decision; and
5. open the existing test partition once only after the complete paired comparison is fixed.
