# Weekly Technical Record

**Week:** 2026-06-29 to 2026-07-05
**Project phase:** Block 3 E0 feasibility audit
**Record started:** 2026-06-30
**Model training performed:** No
**New raw signal data acquired:** No

## 1. Planned Work for This Week

The original timeline assigns this week to the start of E0 feasibility work. The priority is to derive and count REM-to-Wake and Wake-to-REM transition candidates across BOAS PSG `stage_hum` event files, summarize label-quality issues, and decide whether the project should proceed, narrow, redesign, or stop before any model training.

## 2. Work Completed

### 2026-06-30

- Reviewed common AI/ML research-record failure modes and compared them with the current repository structure.
- Added a project-level rule requiring each future model or quantitative baseline experiment to state the known-method baseline, hypothesis, configuration, metric, result, limitation, and next decision.
- Added `docs/planning/ml_experiment_record_requirements.md` as the active checklist for future baseline, transfer, robustness, adaptation, and temporal-localization experiments.
- Updated the overall timeline rules to keep routine setup separate from experiments that test a stated technical uncertainty.
- Confirmed that these updates are documentation guardrails only. No E0 transition inventory, model training, or model evaluation was performed.

### 2026-07-01

- Added `scripts/summarize_boas_e0_transition_inventory.py` to derive deterministic REM/Wake transition candidates from BOAS PSG `stage_hum` event files.
- Ran the inventory across all 128 PSG event tables without downloading new EDF files.
- Found 365 direct REM-to-Wake candidates across 112 recordings and 88 unique `pid` values.
- Found 111 Wake-to-REM secondary candidates across 57 recordings and 46 unique `pid` values.
- Confirmed that the full inventory reproduces the `sub-53` pilot count: four REM-to-Wake candidates and two Wake-to-REM candidates.
- Confirmed that all checked PSG event rows use 30-second epochs, with no missing `stage_hum` values and no PSG/headband duration or sampling-frequency mismatch in sidecars.
- Recorded 37 recordings with PSG disconnection epochs, but no derived REM/Wake candidate window contained a PSG disconnection epoch under the 240-second inspection-window rule.
- Added the E0 inventory outputs and a decision report in `experiments/2026-06-29_to_2026-07-05_boas_e0_transition_inventory/`.
- Recorded a proceed decision for deterministic label-table generation and minimal preprocessing only. No model training or model evaluation was performed.

### 2026-07-04

- Confirmed that only the `sub-53` paired EDF files are currently available locally, so today's alignment work is a pilot validation rather than an all-recording validation.
- Added `scripts/validate_boas_sub53_signal_alignment.py` to test PSG-to-headband signal mapping beyond header and sidecar agreement.
- Verified that the `sub-53` PSG and headband EDF files share the same start time, sampling rate, sample count, duration, and transition-window sample indices.
- Checked all six `sub-53` REM/Wake candidate windows and confirmed that PSG/headband extraction windows use matching sample indices.
- Used `HB_PULSE` versus `PSG_PULSE` as a physiological drift proxy across five 300-second windows; four windows were usable and all usable pulse lags were within +/-2 seconds.
- Used EEG 1-second RMS envelope comparisons as exploratory artifact/physiology proxies; `HB_1` versus `PSG_F3` peaked within +/-1 second for all six transition windows.
- Added the pilot alignment outputs in `experiments/2026-07-04_boas_sub53_signal_alignment/`.
- Recorded a proceed decision for using `sub-53` as a sample-aligned pilot recording only. This result must not be generalized to all BOAS recordings without additional EDF validation.
- Identified the limitation of the pilot approach: it could not answer whether PSG-to-headband sample alignment holds across BOAS because only one EDF pair had been acquired.
- Added `scripts/download_boas_full_edfs.py` as a resumable full-EDF acquisition script that keeps raw EDF files outside Git and records a per-file acquisition manifest.
- Downloaded and size-verified the full BOAS EDF signal set outside Git: 256 EDF files, 35,913,652,480 bytes, with no partial files and no local/expected byte mismatches.
- Added `scripts/validate_boas_full_signal_alignment.py` to test full-dataset PSG-to-headband alignment after acquisition.
- Ran full-dataset alignment validation across 128 paired PSG/headband EDF recordings.
- Confirmed that all 128 EDF pairs matched on timeline fields and all 476 E0 REM/Wake transition windows had matching PSG/headband sample indices.
- Used `HB_PULSE` versus `PSG_PULSE` lag estimates as a physiological drift proxy where channels were present: 101 subjects had pulse channels, 383 pulse windows were usable, and 377 usable windows were within +/-2 seconds.
- Used `HB_1` versus `PSG_F3` 1-second RMS-envelope comparisons around REM/Wake candidates as an exploratory shared-activity proxy: 465 windows were usable and 349 usable windows were within +/-2 seconds.
- Recorded a proceed decision for versioned deterministic transition-label table generation and minimal preprocessing, with model training still blocked until the label/preprocessing gate.
- Added `scripts/build_transition_labels_v0_1.py` to build the first versioned deterministic transition-label artifact from the E0 inventory and full signal-alignment outputs.
- Generated `labels/transition_labels_v0.1/transition_labels_v0.1.tsv` with 476 REM/Wake transition-label rows: 365 primary REM-to-Wake labels and 111 secondary Wake-to-REM labels.
- Preserved each label's nominal boundary, +/-15 second uncertainty interval, PSG/headband boundary sample indices, 240-second extraction-window indices, label source, and quality flags.
- Confirmed that all 476 labels are marked `include` under the current deterministic label/alignment checks, with zero rows requiring review.
- Added `pid_transition_distribution_v0.1.tsv` and `grouped_split_policy_draft_v0.1.md` to support later leakage-safe splitting by `pid` without assigning train/validation/test splits yet.

## 3. Current Technical Position

- The project remains in Block 3, with the count-level E0 inventory, full EDF acquisition, full-dataset signal-alignment validation, and transition-label artifact v0.1 now completed.
- The E0 decision is to proceed to deterministic transition-label generation and minimal preprocessing.
- The next scientific evidence must come from background-window rule definition and recording/window-level signal-quality flags.
- Future ML experiments should not be accepted as meaningful unless they include a baseline-first record, configuration, split, metric, interpretation, and decision.

## 4. Next Planned Work

1. Define background-window rules for non-transition examples without contaminating the uncertainty interval.
2. Add recording-level and transition-window signal-quality flags for label/preprocessing review.
3. Review the grouped split policy using `pid` after background-window rules are defined.
4. Keep model training blocked until the label/preprocessing gate is complete.
